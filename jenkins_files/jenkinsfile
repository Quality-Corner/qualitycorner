pipeline {
    agent {
        dockerfile {
            args '--entrypoint=\'\''
            filename 'Dockerfile'
            dir '/home/jenkins/jenkins_slave/workspace/Docker_Deploy_Learning/docker/base_images/ubuntu_18-04'
            label 'DOCKER'
        }
    }
    stages {
        stage('Deploy') {
            steps {
                sh 'tail -f'
            }
        }
    }
}